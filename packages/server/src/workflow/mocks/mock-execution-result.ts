import { WorkflowExecutionResult } from '@nqframework/models';

export const mockExecutionResult: WorkflowExecutionResult = {
    data: {
        mockProp: "mock value"
    },
    finalOutput: {
        mockFinalOutputProp: "mock final value"
    }
}