import { OrganizationMember } from "../organization/organization-member";

describe("Organization member", () => {
  it("compiles", () => {
    expect({} as OrganizationMember).toBeDefined();
  });
});
